# wordle project

1. Clone repository
2. npm install in **backend** directory
3. Ensure MongoDb is running. 
// E.g. Navigate to mongodb path, run command ./bin/mongod --dbpath ./data (starts mongodb and stores data files in ./data)
4. npm start in **backend** directory
